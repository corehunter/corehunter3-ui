// When using the Eclipse IDE create and update deploy/build.properties to update plugins.p2.dir for eclipse location 
// use the deploy/build.properties.template as an example
// make sure this location is added for the target.
// execute these commands 
mvn p2:site
cd deploy
ant
// Don't forget to refresh the Target Platform in Eclipse
// When using a tycho build these commands are not needed. 
